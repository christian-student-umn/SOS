<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@android:color/transparent</item> <!-- Background area teks -->
        <item name="android:textColor">#000000</item> <!-- Warna teks -->
        <item name="android:textCursorDrawable">@null</item> <!-- Gunakan kursor default -->
    </style>
</resources>
